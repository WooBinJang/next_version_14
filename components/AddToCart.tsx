"use client";

import React from "react";

const AddToCart = () => {
  return (
    <button
      className="btn"
      onClick={() => {
        console.log("!");
      }}
    >
      Add
    </button>
  );
};

export default AddToCart;
